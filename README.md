# Drop Shop MX

Aplicación Next.js para catálogo, validación de agentes, cobros y entrega de objetos digitales.

## Inicio local

```bash
cp .env.example .env.local
npm install
npm run dev
```

La integración real requiere proyectos de Supabase y Mercado Pago, además de una licencia/API key de FN Shop.
